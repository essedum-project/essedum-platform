apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: essedum
  labels:
    app: litellm
spec:
  selector:
    app: litellm
  ports:
    - name: http
      port: 4000
      targetPort: 4000
    - name: nginx-proxy
      port: 9080
      targetPort: 9080
  type: ClusterIP
