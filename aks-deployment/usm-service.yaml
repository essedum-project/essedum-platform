apiVersion: apps/v1
kind: Deployment
metadata:
  name: essedum-backend-usm
  namespace: aipns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: essedum-backend-usm
  template:
    metadata:
      labels:
        app: essedum-backend-usm
    spec:
      containers:
      - name: essedum-backend-usm
        image: ${DOCKER_REGISTRY}/essedum-usm-service:${IMAGE_TAG}
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "200m"
            memory: "384Mi"
          limits:
            cpu: "1000m"
            memory: "1024Mi"
        env:
        - name: MYSQL_DATASOURCE_URL
          valueFrom:
            secretKeyRef:
              name: essedum-db-secret
              key: MYSQL_DATASOURCE_URL
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: essedum-db-secret
              key: MYSQL_USER
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: essedum-db-secret
              key: MYSQL_PASSWORD
        - name: OAUTH2_JWK_URI
          value: "${OAUTH2_JWK_URI}"
        - name: OAUTH2_ISSUER_URI
          value: "${OAUTH2_ISSUER_URI}"
        - name: OAUTH2_CLIENT_ID
          value: "${OAUTH2_CLIENT_ID}"
        - name: SERVICE_ACCESS_TOKEN
          value: "${SERVICE_ACCESS_TOKEN}"
        - name: SERVICE_PROXY_USER
          value: "${SERVICE_PROXY_USER}"
        ports:
        - containerPort: 8081
        startupProbe:
          tcpSocket:
            port: 8081
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 24
        readinessProbe:
          tcpSocket:
            port: 8081
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 6
        livenessProbe:
          tcpSocket:
            port: 8081
          initialDelaySeconds: 50
          periodSeconds: 20
          timeoutSeconds: 3
          failureThreshold: 6
---
apiVersion: v1
kind: Service
metadata:
  name: essedum-backend-usm-service
  namespace: aipns
spec:
  selector:
    app: essedum-backend-usm
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081
  type: ClusterIP
