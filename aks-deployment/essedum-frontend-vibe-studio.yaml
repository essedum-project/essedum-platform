apiVersion: apps/v1
kind: Deployment
metadata:
  name: essedum-frontend-vibe-studio
  namespace: aipns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: essedum-frontend-vibe-studio
  template:
    metadata:
      labels:
        app: essedum-frontend-vibe-studio
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/hostname: essedum-1
      containers:
      - name: essedum-frontend-vibe-studio
        image: ${DOCKER_REGISTRY}/essedum-frontend-vibe-studio:${IMAGE_TAG}
        imagePullPolicy: Always
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: essedum-frontend-vibe-studio-service
  namespace: aipns
spec:
  selector:
    app: essedum-frontend-vibe-studio
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
