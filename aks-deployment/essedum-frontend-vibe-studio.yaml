apiVersion: apps/v1
kind: Deployment
metadata:
  name: essedum-frontend-vibe-studio
  namespace: aipns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: essedum-frontend-vibe-studio
  template:
    metadata:
      labels:
        app: essedum-frontend-vibe-studio
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: essedum-frontend-vibe-studio
        image: acrreq0762935.azurecr.io/essedum-frontend-vibe-studio:v15
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: essedum-frontend-vibe-studio-service
  namespace: aipns
spec:
  selector:
    app: essedum-frontend-vibe-studio
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
