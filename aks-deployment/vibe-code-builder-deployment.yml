
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vibe-code-builder-service
  namespace: aipns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vibe-code-builder
  template:
    metadata:
      labels:
        app: vibe-code-builder
    spec:
      serviceAccountName: builder-sa  # Grants permission per RBAC
      nodeSelector:
        kubernetes.io/hostname: essedum-1
      containers:
        - name: vibe-code-builder
          image: 10.200.111.51:5001/vibe-code-builder-service:lfn-v15
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
          envFrom:
            - secretRef:
                name: vibe-code-builder-secret
          volumeMounts:
            - name: dockerfile-template
              mountPath: /opt/fallback
      volumes:
        - name: dockerfile-template
          configMap:
            name: vibe-code-builder-dockerfile-template
---
apiVersion: v1
kind: Service
metadata:
  name: vibe-code-builder-service
  namespace: aipns
spec:
  selector:
    app: vibe-code-builder
  ports:
    - port: 80
      targetPort: 5000
  type: LoadBalancer
  sessionAffinity: ClientIP
