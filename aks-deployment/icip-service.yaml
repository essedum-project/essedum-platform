apiVersion: apps/v1
kind: Deployment
metadata:
  name: essedum-backend-icip
  namespace: aipns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: essedum-backend-icip
  template:
    metadata:
      labels:
        app: essedum-backend-icip
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/hostname: essedum-1
      hostAliases:
      - ip: "10.200.111.51"
        hostnames:
        - "keycloak.essedum.local"
      containers:
      - name: essedum-backend-icip
        image: ${DOCKER_REGISTRY}/essedum-icip-service:${IMAGE_TAG}
        imagePullPolicy: Always
        env:
        - name: MYSQL_DATASOURCE_URL
          valueFrom:
            secretKeyRef:
              name: essedum-db-secret
              key: MYSQL_DATASOURCE_URL
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: essedum-db-secret
              key: MYSQL_USER
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: essedum-db-secret
              key: MYSQL_PASSWORD
        - name: OAUTH2_JWK_URI
          value: "http://keycloak.aipns.svc.cluster.local:8180/realms/ESSEDUM/protocol/openid-connect/certs"
        - name: OAUTH2_ISSUER_URI
          value: "https://keycloak.essedum.local:30443/realms/ESSEDUM"
        ports:
        - containerPort: 8082
---
apiVersion: v1
kind: Service
metadata:
  name: essedum-backend-icip-service
  namespace: aipns
spec:
  selector:
    app: essedum-backend-icip
  ports:
  - protocol: TCP
    port: 8082
    targetPort: 8082
  type: ClusterIP
