apiVersion: apps/v1
kind: Deployment
metadata:
  name: essedum-frontend-integration
  namespace: aipns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: essedum-frontend-integration
  template:
    metadata:
      labels:
        app: essedum-frontend-integration
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: essedum-frontend-integration
        image: localhost:5000/essedum-frontend-integration:v15
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: essedum-frontend-integration-service
  namespace: aipns
spec:
  selector:
    app: essedum-frontend-integration
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
