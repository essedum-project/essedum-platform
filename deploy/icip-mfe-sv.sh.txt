#!/bin/sh
if [[ $(< /var/local/refresh) == true ]]; then 
set -x
cd mnt
rm -rf *
git clone https://github.com/Infosys-icets-leap/icip-assembly.git
cd icip-assembly
mvn package -s .m2/settings.xml  --batch-mode -DskipTests=true -U
chmod 777 target/icip-app-3.0-SNAPSHOT.zip
unzip -qq target/icip-app-3.0-SNAPSHOT.zip
mv icip-app-3.0-SNAPSHOT /var/local/mnt/app
rm target/icip-app-3.0-SNAPSHOT.zip
cd /var/local/mnt/
ls -la

wget -q --header='Accept:application/octet-stream' -O spark_extrajars.zip  https://infygithub.ad.infosys.com/api/v3/repos/infosys-leap/leap-release/releases/assets/100?access_token=e4182bed78e6f47f7e330ed1f7b682fc193fb8eb
unzip -qq spark_extrajars.zip -d ./app/
rm spark_extrajars.zip


set -x
resultAsJson=$(curl -u ${MAVEN_REPO_USER}:${MAVEN_REPO_PASS} -X POST  https://infyartifactory.ad.infosys.com/artifactory/api/search/aql -H "content-type: text/plain" -d 'items.find({ "repo": {"$eq":"icets-ai_python"}, "name": {"$match" : "leap-2.0*.whl"}})')
latestFile=$(echo $resultAsJson | jq -r '.results | sort_by(.updated) [length-1].name')
latestPath=$(echo $resultAsJson | jq -r '.results | sort_by(.updated) [length-1].path')
whlVersion=$latestFile
wget -q -N -P ./app/python/ --user ${MAVEN_REPO_USER} --password ${MAVEN_REPO_PASS} https://infyartifactory.ad.infosys.com/artifactory/icets-ai_python/$latestPath/$latestFile

mkdir ./app/uploads
mkdir ./app/jobs
mkdir ./app/pluginZips

set +x
resultAsJson=$(curl -u ${MAVEN_REPO_USER}:${MAVEN_REPO_PASS} -X POST  https://infyartifactory.ad.infosys.com:443/artifactory/api/search/aql -H "content-type: text/plain" -d 'items.find({ "repo": {"$eq":"icets-ai_maven"}, "name": {"$match" : "leap-cli-2.2*.zip"}})')
latestFile=$(echo $resultAsJson | jq -r '.results | sort_by(.updated) [length-1].name')
set -x
wget -q -N -P . --user ${MAVEN_REPO_USER} --password ${MAVEN_REPO_PASS} https://infyartifactory.ad.infosys.com:443/artifactory/icets-ai_maven/com/infosys/icets/leap/leap-cli/2.2-SNAPSHOT/$latestFile -O ./app/leap-cli.zip

unzip -qq ./app/leap-cli.zip -d ./app/
rm ./app/leap-cli.zip


set +x
resultAsJson=$(curl -u ${MAVEN_REPO_USER}:${MAVEN_REPO_PASS} -X POST  https://infyartifactory.ad.infosys.com/artifactory/api/search/aql -H "content-type: text/plain" -d 'items.find({ "repo": {"$eq":"icets-ai_maven"}, "name": {"$match" : " icip-adp-mysql-3.0*.zip"}})')
latestFile=$(echo $resultAsJson | jq -r '.results | sort_by(.updated) [length-1].name')
set -x
wget -q -N -P ./app/pluginZips --user ${MAVEN_REPO_USER} --password ${MAVEN_REPO_PASS} https://infyartifactory.ad.infosys.com/artifactory/icets-ai_maven/com/infosys/icets/icip/plugins/icip-adp-mysql/3.0-SNAPSHOT/$latestFile

set +x
resultAsJson=$(curl -u ${MAVEN_REPO_USER}:${MAVEN_REPO_PASS} -X POST  https://infyartifactory.ad.infosys.com/artifactory/api/search/aql -H "content-type: text/plain" -d 'items.find({ "repo": {"$eq":"icets-ai_maven"}, "name": {"$match" : " icip-adp-file-3.0*.zip"}})')
latestFile=$(echo $resultAsJson | jq -r '.results | sort_by(.updated) [length-1].name')
set -x
wget -q -N -P ./app/pluginZips --user ${MAVEN_REPO_USER} --password ${MAVEN_REPO_PASS} https://infyartifactory.ad.infosys.com/artifactory/icets-ai_maven/com/infosys/icets/icip/plugins/icip-adp-file/3.0-SNAPSHOT/$latestFile

set +x
resultAsJson=$(curl -u ${MAVEN_REPO_USER}:${MAVEN_REPO_PASS} -X POST  https://infyartifactory.ad.infosys.com/artifactory/api/search/aql -H "content-type: text/plain" -d 'items.find({ "repo": {"$eq":"icets-ai_maven"}, "name": {"$match" : " icip-adp-rest-3.0*.zip"}})')
latestFile=$(echo $resultAsJson | jq -r '.results | sort_by(.updated) [length-1].name')
set -x
wget -q -N -P ./app/pluginZips --user ${MAVEN_REPO_USER} --password ${MAVEN_REPO_PASS} https://infyartifactory.ad.infosys.com/artifactory/icets-ai_maven/com/infosys/icets/icip/plugins/icip-adp-rest/3.0-SNAPSHOT/$latestFile

set +x
resultAsJson=$(curl -u ${MAVEN_REPO_USER}:${MAVEN_REPO_PASS} -X POST  https://infyartifactory.ad.infosys.com/artifactory/api/search/aql -H "content-type: text/plain" -d 'items.find({ "repo": {"$eq":"icets-ai_maven"}, "name": {"$match" : " icip-adp-servicenow-2.2*.zip"}})')
latestFile=$(echo $resultAsJson | jq -r '.results | sort_by(.updated) [length-1].name')
set -x
wget -q -N -P ./app/pluginZips --user ${MAVEN_REPO_USER} --password ${MAVEN_REPO_PASS} https://infyartifactory.ad.infosys.com/artifactory/icets-ai_maven/com/infosys/icets/icip/plugins/icip-adp-servicenow/2.2-SNAPSHOT/$latestFile

set +x
resultAsJson=$(curl -u ${MAVEN_REPO_USER}:${MAVEN_REPO_PASS} -X POST  https://infyartifactory.ad.infosys.com/artifactory/api/search/aql -H "content-type: text/plain" -d 'items.find({ "repo": {"$eq":"icets-ai_maven"}, "name": {"$match" : " icip-adp-h2-3.0*.zip"}})')
latestFile=$(echo $resultAsJson | jq -r '.results | sort_by(.updated) [length-1].name')
set -x
wget -q -N -P ./app/pluginZips --user ${MAVEN_REPO_USER} --password ${MAVEN_REPO_PASS} https://infyartifactory.ad.infosys.com/artifactory/icets-ai_maven/com/infosys/icets/icip/plugins/icip-adp-h2/3.0-SNAPSHOT/$latestFile

wget -q -N -P ./app/python --user ${MAVEN_REPO_USER} --password ${MAVEN_REPO_PASS} https://infyartifactory.ad.infosys.com:443/artifactory/icets-ai_generic/nltk_data.zip
unzip ./app/python/nltk_data.zip -d ./app/python
rm ./app/python/nltk_data.zip
chmod -R 777 ./app
cd ..
fi
ls mnt
echo $leapVersion
echo $uiVersion
echo $whlVersion